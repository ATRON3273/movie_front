<!DOCTYPE html>
<html lang="en">
<%- include('../_component/head.ejs', {titleName: '관리자 로그인'})%>
<body>
    <a href="/">관리자 메인 화면</a>
    <a href="/">메인 화면</a>
    관리자 로그인 페이지
    ID
    <input type="id" name="admin_signin_id" id="input_admin_id">
    비밀번호
    <input type="password" name="admin_signin_pw" id="input_admin_pw">
    <button id="btn_admin_signin">로그인</button>
    
    <script>
        const btn_signin = findID('btn_admin_signin');
        const input_id = findID('input_admin_id');
        const input_pw = findID('input_admin_pw');

        btn_signin.addEventListener('click', function(){
            const data = {
                id: input_id.value,
                pw: input_pw.value,
            };
            requestPostAPI('/admin/signin', data)
            .then(function (response) {
                if (response.data.result)
                {
                    alert('로그인 성공');
                    window.location.href = '/admin';
                }
                else
                {
                    alert('로그인 실패');
                }
            });
        });
    </script>
</body>
</html>